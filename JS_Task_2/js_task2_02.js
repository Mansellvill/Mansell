function checkArr(arr){
    for (var i = 0; i<arr.length;i++){
        console.log(arr[i]);
    }
    console.log('Число элементов в массиве: ' + arr.length);
}

var arr  = [100, 101, 102, 103,104];
checkArr(arr);
